# 16 MB flash, partition table at 0x10000 (see sdkconfig.defaults) rather than
# the stock 0x8000: that left the bootloader only 24K, of which it already used
# 96%. Two OTA app slots (see main/ota.c) replace the old single 4MB factory
# slot -- same total app flash, just split in half. nvs/storage keep their old
# offsets and sizes on purpose: this partition-table change does not force an
# NVS or spiffs erase on top of it. `storage` has no mount anywhere in this
# project (grepped, confirmed 2026-09-08); it is untouched headroom, not in use.
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x11000,  0x6000,
otadata,    data, ota,     0x17000,  0x2000,
phy_init,   data, phy,     0x19000,  0x1000,
ota_0,      app,  ota_0,   0x20000,  0x200000,
ota_1,      app,  ota_1,   0x220000, 0x200000,
storage,    data, spiffs,  0x420000, 0xBE0000,
