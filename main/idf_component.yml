## IDF Component Manager Manifest File
dependencies:
  idf:
    version: '>=6.0'
  espressif/usb: ^1
  # espressif/es8311 is deprecated upstream and declares idf <6.0;
  # esp_codec_dev is the maintained replacement and uses the new I2C driver.
  espressif/esp_codec_dev: ^1.6
